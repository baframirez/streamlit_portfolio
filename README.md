# Data Scientist Portfolio

A Streamlit application showcasing projects, interactive demos, and contact information.

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
```

## Deployment

This app can be deployed to Streamlit Cloud using the `app.py` entrypoint.
